{
  "buildCommand": "cd public && npm install && CI=false npm run build",
  "outputDirectory": "public/build",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
